{
    "name": "netspy",
    "version": "1.3.0",
    "description": "Network Request Interceptor for Chrome DevTools",
    "scripts": {
        "build": "node build.js",
        "build:prod": "node build.js --minify"
    },
    "devDependencies": {
        "terser": "^5.27.0",
        "clean-css": "^5.3.3",
        "archiver": "^6.0.1"
    }
}